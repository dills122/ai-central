interface:
  display_name: "GitHub Keychain Auth"
  short_description: "Use GitHub Keychain credentials without leaks"
  default_prompt: "Use $github-keychain-auth to authenticate this GitHub operation safely."
